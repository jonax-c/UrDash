{
  "config": {
    "abort": {
      "single_instance_allowed": "UrDash is already configured."
    },
    "step": {
      "user": {
        "title": "Set up UrDash",
        "description": "Create the UrDash v2 card designer panel.",
        "data": {
          "api_key": "API key",
          "model": "Model",
          "base_url": "API base URL",
          "default_theme": "Default card theme",
          "default_height_mode": "Default height mode"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "UrDash options",
        "description": "Configure the OpenAI-compatible provider used by the v2 card design agent.",
        "data": {
          "api_key": "API key",
          "model": "Model",
          "base_url": "API base URL",
          "default_theme": "Default card theme",
          "default_height_mode": "Default height mode"
        }
      }
    }
  }
}
